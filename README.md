# Rails Video Store example app

## Development

First, install Postgres. Then:

```bash
rails db:create
rails db:migrate
rails server
```

The admin interface is then available at [localhost:3000/admin](http://localhost:3000/admin)
